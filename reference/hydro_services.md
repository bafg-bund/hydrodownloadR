# List available providers

List available providers

## Usage

``` r
hydro_services()
```

## Value

A tibble with columns: provider_id, provider_name, country, base_url,
license, license_link, access_class, reuse_class, is_open_data
